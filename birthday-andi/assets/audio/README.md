# Audio

The gentle birthday music is generated in-browser with the Web Audio API from `js/main.js`.
This keeps the project self-contained, royalty-free, lightweight, and playable by opening `index.html`.
